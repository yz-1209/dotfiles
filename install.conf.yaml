- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.tmux.conf: tmux.conf
    ~/.vimrc: vimrc
    ~/.inputrc: inputrc
    ~/.gitconfig: gitconfig
    ~/.ideavimrc: ideavimrc
